import type { Metadata } from "next";
export const metadata: Metadata = { title: "Criar Conta - CheerConnect" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
