import type { Metadata } from "next";
export const metadata: Metadata = { title: "Gerenciar Equipe - CheerConnect" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
